interface:
  display_name: "Appium Interactive"
  short_description: "Persistent Android Appium QA"
  default_prompt: "Use $appium-interactive to run preflight-lite first, keep work inside the selected runtime anchor, and fail fast on invalid target shapes or runtime-anchor drift."
